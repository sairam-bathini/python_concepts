# The def keyword in Python is used to define a function. It allows you to create a reusable block of code that can be executed when called.

def access_elements(my_list):
  print(my_list[0])

example_list = [10, 20, 30, 40)
access_elements(example_list)
